alert("Hello world!");
// Method alert() merupakan salah satu method BOM yang menerima satu parameter opsional berupa data string yang akan ditampilkan melalui sebuah pop-up browser.

let pesanInput = prompt("Masukan Pesanmu ...");
// Method ini akan menampilkan sebuah dialog browser yang meminta user untuk mengisi kolom inputnya.

console.log("Hello World!");
// Melalui objek console ini, kita dapat membuat kode JavaScript menampilkan pesan-pesan khusus berdasarkan konteks tertentu pada console browser.

// Method console yang umum digunakan
/**
 * log() = Menampilkan pesan secara umum ke console browser
 * info() = Menampilkan pesan yang berisi informasi penting ke console browser
 * warn() = Menampilkan pesan yang berisi informasi dalam bentuk peringatan ke console browser
 * error() = Menampilkan pesan yang berisi informasi dalam bentuk error ke console browser
 *
 * jika pada scope sebuah berkas .js terdapat nama fungsi yang sama, maka pesan pada pop-up tidak akan muncul.
 */
